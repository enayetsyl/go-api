export type Feature = {
  id: number;
  icon: string;
  title: string;
  description?: string;
  image?: boolean;
  buttonText?: string;
  placeholder?: string;
  transcript?: boolean;
  prompt?: string;
  response?:string;
};
